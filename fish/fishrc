set -g  EDITOR=nvim

# Path to your oh-my-zsh installation.
set -g  ZSH $HOME/.oh-my-zsh
set -g  PRIV $HOME/.zshrcpriv
set -g  DOTFILES $HOME/.dotfiles/zsh
set -g  MYVIMRC $HOME/.vimrc

# TODO aliases 

sh $PRIV

# FZF bindings
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh
